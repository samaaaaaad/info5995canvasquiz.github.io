weeks[10].canvas = [
  { id: "w11-canvas-1", type: "MCQ", prompt: "Rice's theorem says that no automatic tool can perfectly decide every non-trivial semantic property of every possible program.\nWhat is the most practical lesson for security analysis?", options: ["Perfect analysis is impossible, so tools must make trade-offs", "AI tools are exempt from undecidability because they use learned patterns", "Dynamic testing can always replace all static analysis", "Program analysis is useless and should not be used"], correct: [0], feedback: "Perfect analysis is impossible, so practical tools trade precision, coverage, and cost." },
  { id: "w11-canvas-2", type: "MCQ", prompt: "A bug detector warns that a vending machine can dispense a snack without payment.\nAfter investigation, the warning is impossible because a separate payment lock always prevents it.\nWhat kind of result is this?", options: ["True positive", "False positive", "False negative", "True negative"], correct: [1], feedback: "The tool reported a problem that was not actually possible, so it is a false positive." },
  { id: "w11-canvas-3", type: "MCQ", prompt: "Static analysis reads code without running it.\nWhich vending-machine analogy best matches static analysis?", options: ["Building a simplified toy model and checking every state in that model", "Reading the machine's blueprint and reasoning about what might happen", "Randomly pressing buttons until the machine crashes", "Turning on the machine and buying a snack"], correct: [1], feedback: "Static analysis reasons from code or structure without executing the program." },
  { id: "w11-canvas-4", type: "MCQ", prompt: "Fuzzing runs a program with many unexpected inputs, such as empty files, very long strings, corrupted files, or random bytes.\nWhat is fuzzing mainly trying to find?", options: ["A complete list of every possible program state", "A mathematical proof that the program is correct for all inputs", "A guarantee that no hidden bug exists", "A real input that causes the program to crash or violate an expected behavior"], correct: [3], feedback: "Fuzzing searches for concrete inputs that trigger crashes or other unexpected behavior." },
  { id: "w11-canvas-5", type: "MCQ", prompt: "Model checking builds a model of a system, such as states `Idle`, `Paid`, `Dispensing`, and `Error`, then checks whether a property can be violated in that model.\nWhat is a key limitation of model checking?", options: ["It always gives false positives and never gives counterexamples", "It only works if the model accurately captures the relevant behavior", "It never explores more than one state", "It cannot check any safety property"], correct: [1], feedback: "Model checking is only as useful as the model's fidelity to the behavior being checked." },
  { id: "w11-canvas-6", type: "MAQ", prompt: "Which statements about over-approximation and under-approximation are correct?", options: ["Under-approximation explores only some real behaviors and may miss real bugs", "Over-approximation includes all real behaviors but may also include impossible behaviors", "Over-approximation guarantees no false positives in bug finding", "Under-approximation proves that no bugs exist if it finds no bug"], correct: [0, 1], feedback: "Under-approximation can miss bugs; over-approximation can include impossible behaviors." },
  { id: "w11-canvas-7", type: "MAQ", prompt: "Symbolic or concolic execution can start with unknown variables, such as `coin = X`, `button = Y`, and `coupon = Z`, follow program logic, collect conditions, and solve them into real inputs.\nWhich statements are correct?", options: ["It tries to solve for inputs that can reach specific paths", "It proves that every possible behavior of the whole program is safe", "It does not need to know the program logic or structure", "It can suffer from path explosion when a program has many branches and loops"], correct: [0, 3], feedback: "Symbolic and concolic execution solve path constraints, but path explosion can limit coverage." },
  { id: "w11-canvas-8", type: "MAQ", prompt: "Which statements about AI-assisted program analysis are correct?", options: ["It can act like a human-like investigator by looking for suspicious logic, missing authorization checks, or risky API usage", "Its findings should be treated as hypotheses that need validation", "It gives the same formal guarantee as exhaustive model checking", "It cannot be combined with static analysis, fuzzing, or tests"], correct: [0, 1], feedback: "AI-assisted findings are useful hypotheses, but they still need validation." },
  { id: "w11-canvas-9", type: "MAQ", prompt: "Which statements about human factors and usable security are correct?", options: ["Humans may over-trust dashboards, warning lights, green checkmarks, or transaction history, especially under stress", "If a system uses strong cryptography, human interface design no longer matters", "A secure interface should communicate the real system state clearly, not just what the system or user intended to do", "Adding more warnings and prompts always improves security without usability cost"], correct: [0, 2], feedback: "Usable security depends on clearly communicating real system state and avoiding misleading trust signals." },
  { id: "w11-canvas-10", type: "MAQ", prompt: "Which statements about responsible security research and risk management are correct?", options: ["If a researcher unexpectedly accesses sensitive user data, they should continue testing until they fully understand the system", "A responsible workflow includes validating carefully, minimizing harm, reporting privately, and giving time to fix", "Risk management is only a mathematical calculation and never involves human judgment or trade-offs", "Good intentions do not automatically make unauthorized testing legal or safe"], correct: [1, 3], feedback: "Responsible research minimizes harm and good intentions do not remove legal or safety risk." }
];

weeks[11].canvas = [
  { id: "w12-canvas-1", type: "MCQ", prompt: "A student asks an LLM to review a decompiled Android app.\nThe model claims that an admin-only screen can be opened from outside the app through a deep link.\nWhat is the best next step before treating this as a reportable vulnerability?", options: ["Ignore the issue because Android deep links can only be opened by the app vendor's website", "Check the manifest and handler code, then build a minimal reproduction that shows the external entry point and missing authorization", "Assume the Android sandbox prevents any exported component from affecting app state", "Publish the claim immediately because decompiled code is always exact"], correct: [1], feedback: "Validate the claim with manifest/code review and a minimal proof of concept before reporting." },
  { id: "w12-canvas-2", type: "MCQ", prompt: "A reset-token service computes `SHA-256(username || current_minute || 4_digit_code)`.\nThe digest is 256 bits long, but the attacker knows the username list and can try likely minutes and all 4-digit codes.\nWhich assessment is most accurate?", options: ["The token is equivalent to a one-time pad key because it is derived by hashing", "Kerckhoffs's principle says this is secure as long as SHA-256 is public", "The token has 256 bits of real entropy because SHA-256 outputs 256 bits", "The token can be weak because the input space is enumerable even though the digest is long"], correct: [3], feedback: "A long hash output does not add entropy when the input space is predictable and enumerable." },
  { id: "w12-canvas-3", type: "MCQ", prompt: "A protocol sends `C = Enc(k_enc, M)` and `T = MAC(k_mac, M)`.\nThe receiver decrypts `C` first and then checks the MAC over the recovered plaintext.\nWhich change best matches the encrypt-then-MAC design discussed in class?", options: ["Reuse the encryption key as the MAC key so there is only one secret", "Replace the MAC with `SHA-256(M)` because hashes are public", "Compute `T = MAC(k_mac, C)` and verify `T` before decrypting `C`", "Decrypt first, then ignore the MAC if the plaintext looks readable"], correct: [2], feedback: "Encrypt-then-MAC authenticates the ciphertext before decryption." },
  { id: "w12-canvas-4", type: "MCQ", prompt: "During a TLS-like handshake, the server sends an ephemeral Diffie-Hellman public value.\nAn active attacker wants to replace that value with their own.\nWhat prevents this substitution in an authenticated ECDHE-style handshake?", options: ["The client hides all Diffie-Hellman public values from the network", "The certificate proves the web application has no SQL injection or XSS bugs", "The server binds the ephemeral value to its authenticated identity, for example with a signature validated through the certificate chain", "TCP encrypts the ephemeral value before TLS sees it"], correct: [2], feedback: "The server authenticates the ephemeral key exchange value, commonly with a signature tied to the certificate." },
  { id: "w12-canvas-5", type: "MCQ", prompt: "A static analyzer reports an authorization bypass on a path that later turns out to require an impossible combination of states.\nA fuzzer runs many inputs and does not find the bypass.\nWhat is the most accurate interpretation?", options: ["The static warning must be a true positive because static analysis is always exact", "The fuzzer proves there is no bug because it executed many inputs", "The static warning may be a false positive, and the fuzzer's failure to find a bug does not prove the program is safe", "Both tools are useless because perfect program analysis is impossible"], correct: [2], feedback: "Static analysis can over-approximate, and fuzzing is not proof that no bug exists." },
  { id: "w12-canvas-6", type: "MAQ", prompt: "Which statements about Android component exposure and deep links are correct?", options: ["Once an activity is exported, Android automatically performs all app-specific authorization checks", "`android.intent.category.BROWSABLE` guarantees that only the app vendor's own domain can invoke the activity", "External intent and deep-link parameters should be treated as untrusted input and validated before use", "An activity intended only for internal app navigation is usually safer with `android:exported=\"false\"`"], correct: [2, 3], feedback: "External inputs need validation, and internal-only activities should generally not be exported." },
  { id: "w12-canvas-7", type: "MAQ", prompt: "Two six-bit plaintexts are encrypted with the same XOR keystream.\nThe attacker sees `C1 = 001111` and `C2 = 010010`, and later learns that `M1 = 101010`.\nWhich statements are correct?", options: ["Knowing one plaintext still gives no information about the reused keystream", "`C1 xor C2` directly reveals both plaintexts even without any known plaintext", "The attacker can compute `M1 xor M2 = C1 xor C2 = 011101`", "The attacker can recover `M2 = 110111` for these six bits"], correct: [2, 3], feedback: "Reusing an XOR keystream leaks plaintext relationships; known plaintext can recover the paired plaintext." },
  { id: "w12-canvas-8", type: "MAQ", prompt: "A web app uses HTTPS with a valid certificate, but login input is concatenated into SQL and profile comments are rendered as raw HTML.\nWhich defenses directly address these application-layer bugs?", options: ["Rely on HTTPS to prevent SQL injection and stored XSS inside the server application", "Store passwords with a faster general-purpose hash so the page renders more quickly", "Use prepared statements or parameterized queries for database access", "Encode untrusted output before rendering it into HTML"], correct: [2, 3], feedback: "Prepared statements address SQL injection, and output encoding addresses XSS." },
  { id: "w12-canvas-9", type: "MAQ", prompt: "Which statements about consensus, blockchains, and DeFi are correct?", options: ["A longest-chain or most-work rule helps nodes converge after temporary blockchain forks", "Raft assumes open anonymous membership where anyone can create unlimited identities", "In a permissionless system, Proof of Work can make influence depend on costly work rather than trusted real-world identities", "Slippage protection is designed to maximize the victim's loss during a sandwich attack"], correct: [0, 2], feedback: "PoW uses costly work in permissionless settings, and longest-chain/most-work rules help resolve forks." },
  { id: "w12-canvas-10", type: "MAQ", prompt: "Which statements about security analysis and responsible research are correct?", options: ["Responsible disclosure means publishing full exploit details before contacting the affected party", "If testing unexpectedly exposes sensitive user data, the researcher should stop, minimize harm, and report through an appropriate private channel", "AI-assisted findings should be treated as hypotheses that need evidence such as code review, tests, traces, or a safe proof of concept", "A false positive means a real bug existed but the tool failed to report it"], correct: [1, 2], feedback: "Responsible reporting minimizes harm, and AI-assisted findings need evidence before being treated as confirmed." }
];

window.COMP6007_CANVAS_11_12_CHECK = { week11: weeks[10].canvas.length, week12: weeks[11].canvas.length };
if (typeof render === "function") render();
